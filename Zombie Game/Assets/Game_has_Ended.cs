using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Game_has_Ended : MonoBehaviour
{
    Game_Manager game_has_eneded;
    // Start is called before the first frame update
   

    //// Update is called once per frame
    //void Update()
    //{
    //    if (game_has_eneded.GameEnded == true)
    //        Destroy(gameObject);
    //}
}
